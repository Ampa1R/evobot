name: Deploy Production Wagner

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: "just-a-placeholder-so-we-dont-get-errors"
      - name: Adding Known Hosts
        run: ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Copy sources to server
        run: rsync -avz ./ root@${{ secrets.SSH_HOST }}:/home/ci/apps/evobot_wagner_fresh
      - name: Build & run application
        run: >-
          ssh root@${{ secrets.SSH_HOST }}
          '
            uptime;
            cd /home/ci/apps;
            rm -rf evobot_wagner_prev;
            mv evobot_wagner evobot_wagner_prev;
            mv evobot_wagner_fresh evobot_wagner;
            cp envs/evobot_wagner.config.json evobot_wagner/config.json;
            cd evobot_wagner;
            docker compose build --no-cache;
            docker compose up -d;
          '
